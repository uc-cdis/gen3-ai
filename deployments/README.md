# Deployment-related scripts and configuration

`k8s` supports a container-based, Kubernetes deployment of the
services as persistently-running web-based Python applications.

Future ideas:

- `aws`: Function-as-a-service (FaaS) functions that can be deployed to AWS Lambda
